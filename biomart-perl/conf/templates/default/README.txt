These templates form the base of the MartView web interface. Some are used
in the pre-generation phase, to create dataset-specific secondary templates
(filters, attributes). Others are used directly by the CGI-request handling code.
